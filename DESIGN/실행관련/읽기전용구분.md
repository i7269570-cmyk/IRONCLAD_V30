 🔒 1️⃣ “읽기 전용(LOCK)” 걸어야 하는 파일

👉 이건 **완전히 고정 (수정 금지)**

RUNTIME/run.py
RUNTIME/entry_engine.py
RUNTIME/exit_engine.py
RUNTIME/risk_gate.py
RUNTIME/position_reconciler.py
RUNTIME/state_manager.py
RUNTIME/pre_order_check.py
RUNTIME/order_manager.py
RUNTIME/fill_tracker.py
RUNTIME/ledger_writer.py
RUNTIME/selector.py
RUNTIME/regime_filter.py


LOCKED/system_config.yaml
LOCKED/schema.json
LOCKED/recovery_policy.yaml


STRATEGY/selection_rules.yaml
STRATEGY/regime_rules.yaml
STRATEGY/*/entry_rules.yaml
STRATEGY/*/exit_rules.yaml


👉 이유
구조 / 전략 / 리스크 / 상태 = 이미 완성 → 건드리면 다시 지옥 시wkr

🔓 2️⃣ 읽기만 하면 안 되는 (수정 가능해야 하는 영역)

👉 여기는 **API 연결 때문에 수정 필요**

RUNTIME/data_loader.py   ← API 데이터 매핑
RUNTIME/exception_handler.py ← 에러 처리
RUNTIME/scheduler.py     ← 시간 조정 가능
UNIVERSE/build_universe.py ← 종목 갱신

👉 이유

실전 연결하면 100% 바뀌는 영역

⚠️ 3️⃣ 절대 “읽기 고정하면 안 되는 핵심”

👉 이거 잘못 잠그면 실전 못 돌아간다

data_loader.py ❌ (가장 중요) - 이유: LS증권 / 업비트 응답 구조 바뀜 → 반드시 수정 필요

order_manager.py ❌ (API 붙일 때) - 이유: 실제 주문 코드 들어감


fill_tracker.py ❌ - 이유: 체결 응답 구조 맞춰야 함

exception_handler.py ❌ - 이유: API 에러 처리 필요


# 🎯 핵심 구분 (중요)

고정해야 할 것: → "판단 로직"

고정하면 안 되는 것: → "외부 연결 로직"

# 🔐 추천 실제 적용 방법 (윈도우)

## 1️⃣ 읽기 전용 설정

```powershell
attrib +R RUNTIME\run.py
attrib +R RUNTIME\entry_engine.py

## 2️⃣ 폴더 단위 잠금

LOCKED 폴더 전체 읽기전용
STRATEGY 폴더 전체 읽기전용

# 🔥 한 줄 핵심 전략/구조는 잠그고,
API 연결부만 열어둬라

