

### ✅ V30.2 최종 감사 순서 (3.22 스타일)

**파일 만드는 순서 + 각 단계 후 감사 대상**

1. **run.py**  
   → 감사: run.py 단독 (임시)

2. **scheduler.py**  
   → 감사: run.py + scheduler.py (2개)

3. **risk_gate.py**  
   → 감사: run.py + scheduler.py + risk_gate.py (3개) → PASS 보관

4. **preflight_gate.py**  
   → 감사: run.py + preflight_gate.py (2개)

5. **integrity_guard.py**  
   → 감사: run.py + preflight_gate.py + integrity_guard.py (3개) → PASS 보관

6. **state_manager.py**  
   → 감사: run.py + scheduler.py + risk_gate.py + preflight_gate.py + integrity_guard.py + state_manager.py (6개)

7. **exception_handler.py**  
   → 감사: 위 6개 + exception_handler.py (7개) → PASS 보관

8. **data_loader.py**  
   → 감사: 위 7개 + data_loader.py (8개)

9. **selector.py**  
   → 감사: 위 8개 + selector.py (9개)

10. **regime_filter.py**  
    → 감사: 위 9개 + regime_filter.py (10개)

11. **entry_engine.py**  
    → 감사: 위 10개 + entry_engine.py (11개) → 핵심 RUNTIME 11개 PASS 보관

12. **pre_order_check.py**  
    → 감사: 위 11개 + pre_order_check.py (12개)

13. **order_manager.py**  
    → 감사: 위 12개 + order_manager.py (13개)

14. **fill_tracker.py**  
    → 감사: 위 13개 + fill_tracker.py (14개)

15. **ledger_writer.py**  
    → 감사: 위 14개 + ledger_writer.py (15개)

16. **exit_engine.py**  
    → 감사: 위 15개 + exit_engine.py (16개)

17. **position_reconciler.py**  
    → 감사: 위 16개 + position_reconciler.py (17개) → RUNTIME 전체 17개 PASS 보관

18. **LOCKED/system_config.yaml**  
    → 감사: RUNTIME 17개 + system_config.yaml (18개)

19. **LOCKED/schema.json**  
    → 감사: 위 18개 + schema.json (19개)

20. **LOCKED/recovery_policy.yaml**  
    → 감사: 위 19개 + recovery_policy.yaml (20개) → LOCKED 완료 PASS 보관

21. **UNIVERSE/build_universe.py**  
    → 감사: 위 20개 + build_universe.py (21개)

22. **STRATEGY/data_rules.yaml**  
    → 감사: 위 21개 + data_rules.yaml (22개)

23. **STRATEGY/selection_rules.yaml**  
    → 감사: 위 22개 + selection_rules.yaml (23개)

24. **STRATEGY/regime_rules.yaml**  
    → 감사: 위 23개 + regime_rules.yaml (24개)

**STRATEGY 하위 전략별 (마지막)**  
25. **STRATEGY/stock_A/entry_rules.yaml**  
26. **STRATEGY/stock_A/exit_rules.yaml**  
27. **STRATEGY/stock_B/entry_rules.yaml**  
28. **STRATEGY/stock_B/exit_rules.yaml**  
29. **STRATEGY/crypto_A/entry_rules.yaml**  
30. **STRATEGY/crypto_B/entry_rules.yaml** 등...

31. **최종 통합 감사** (전체 30+개 파일)

---

이 순서대로 하면 **모든 파일이 감사에 포함**되며, 3.22처럼 단계별로 PASS 보관 가능합니다.

사용하시겠습니까?