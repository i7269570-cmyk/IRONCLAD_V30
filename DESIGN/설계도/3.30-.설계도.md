IRONCLAD_V30.2_FINAL/

├─ LOCKED/                                              # 절대 규칙 영역 (수정 금지, SSOT)
│  ├─ system_config.yaml                                # 리스크 상한선 (포지션 수, 노출, 손실 제한)
│  ├─ schema.json                                       # config 구조 검증 (additionalProperties: false)
│  ├─ recovery_policy.yaml                              # SAFE_HALT 정책 (자동 복구 금지)
│  └─ GUARDS/
│     ├─ preflight_gate.py                              # 실행 전 검사 (파일/설정/state 검증)
│     └─ integrity_guard.py                             # 실행 중 무결성 감시

├─ STRATEGY/                                            # 전략 전용 영역 (유일 변경 가능)
│
│  ├─ stock_A/                                          # 주식 전략 A
│  │   ├─ entry_rules.yaml                              # 진입 조건
│  │   └─ exit_rules.yaml                               # 청산 조건
│
│  ├─ stock_B/                                          # 주식 전략 B
│  │   ├─ entry_rules.yaml
│  │   └─ exit_rules.yaml
│
│  ├─ crypto_A/                                         # 코인 전략 A
│  │   ├─ entry_rules.yaml
│  │   └─ exit_rules.yaml
│
│  ├─ crypto_B/                                         # 코인 전략 B
│  │   ├─ entry_rules.yaml
│  │   └─ exit_rules.yaml
│
│  ├─ data_rules.yaml                                   # (기존 strategy_spec.yaml 역할)
│  │                                                    # 전체 전략 공통 파라미터 (시간/자산/포지션 규칙)
│
│  ├─ selection_rules.yaml                              # 종목 선정 기준 (Top-N → 3~5개)
│
│  └─ regime_rules.yaml                                 # 시장 상태 필터 (진입 허용/차단)

├─ UNIVERSE/                                            # 종목 생성 영역 (실행과 분리)
│  └─ build_universe.py                                 # 🔥 08:00 종목 선정 실행
│                                                       # 거래대금 상위 → Top-N → 저장

├─ RUNTIME/                                             # 실행 엔진 (코드 영역)
│  ├─ run.py                                            # 전체 파이프라인 제어 (유일 진입점)
│  ├─ scheduler.py                                      # 시간 모드 결정
│  │                                                    # TRADE / NO_ENTRY / FORCE_EXIT / CLOSED
│  ├─ data_loader.py                                    # UNIVERSE 기반 데이터 로드 (전종목 금지)
│  ├─ selector.py                                       # Top-N → 최종 3~5 종목 선택
│  ├─ regime_filter.py                                  # 시장 상태 평가
│  ├─ entry_engine.py                                   # 🔥 전략 폴더 순회 (stock_A/B, crypto_A/B)
│  │                                                    # strategy_name 반드시 포함
│  ├─ risk_gate.py                                      # 리스크 검사 + 포지션 크기 계산
│  │                                                    # max_positions / max_per_asset / exposure 적용
│  ├─ pre_order_check.py                                # 주문 직전 검증
│  ├─ order_manager.py                                  # 주문 실행 (idempotent 처리)
│  ├─ fill_tracker.py                                   # 체결 추적
│  ├─ ledger_writer.py                                  # 거래 기록 저장
│  ├─ exit_engine.py                                    # 청산 로직 (TP/SL/시간/강제청산)
│  ├─ position_reconciler.py                            # state ↔ 실제 상태 동기화
│  ├─ state_manager.py                                  # 🔥 atomic write (temp → fsync → replace)
│  └─ exception_handler.py                              # 🔥 SAFE_HALT (Cancel-All + 종료)

├─ STATE/                                               # 상태 저장 영역
│  └─ state.json                                        # 현재 포지션 상태

└─ EVIDENCE/                                            # 증거 기록 영역
   ├─ ledger/                                           # 거래 로그
   └─ incident/                                         # 사고 기록 (SAFE_HALT)