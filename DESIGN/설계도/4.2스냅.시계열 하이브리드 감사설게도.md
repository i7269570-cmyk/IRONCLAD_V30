**확정 감사 순서** (시계열 포함, 그대로 진행):

1. data_loader.py  
2. STRATEGY/data_rules.yaml  
3. LOCKED/system_config.yaml  
4. indicator_calc.py  
5. entry_engine.py  
6. exit_engine.py  
7. state_manager.py  
8. risk_gate.py  
9. run.py + scheduler.py  
10. selector.py + regime_filter.py  
11. pre_order_check.py  
12. order_manager.py  
13. fill_tracker.py  
14. ledger_writer.py  
15. position_reconciler.py  
16. LOCKED/schema.json  
17. LOCKED/recovery_policy.yaml  
18. UNIVERSE/build_universe.py  
19. STRATEGY/selection_rules.yaml  
20. STRATEGY/regime_rules.yaml  
21. STRATEGY/* entry_rules.yaml / exit_rules.yaml  
22. 최종 통합 감사

**고정 규칙**:  
data_loader: current + history 반환  
entry/exit: history 판단 + current 실행  
state_manager: 절대 수정 금지


# 🔒 절대 규칙 재확인
1. data_loader = current + history 반드시 둘 다

2. entry/exit
   판단 → history
   실행 → current

3. state_manager
   수정 금지 (절대 기준)

# 🚨 중단 조건

run.py 연결 깨짐
→ 즉시 중단
→ 그 단계에서 해결 후 진행

