핵심 실행 흐름 (최종 고정)


[사전]
08:00 → UNIVERSE/build_universe.py 실행
       → 거래대금 상위 종목 선정 저장

[실행]
1. run.py 시작

2. preflight_gate.py
   → 필수 파일 / config / state 검사

3. integrity_guard.py
   → LOCKED 무결성 감시

4. scheduler.py
   → 현재 모드 결정

5. data_loader.py
   → UNIVERSE 기반 Top-N만 로드

6. selector.py
   → 최종 3~5 종목 선정

7. regime_filter.py
   → 시장 진입 가능 여부 판단

8. entry_engine.py
   → stock_A / stock_B / crypto_A / crypto_B 순회
   → 조건 충족 시 BUY 생성
   → strategy_name 포함 필수

9. risk_gate.py
   → 최대 2포지션
   → 동일 자산군 금지
   → 총 노출 제한
   → position sizing

10. pre_order_check.py
    → 주문 직전 검증

11. order_manager.py
    → 주문 실행

12. fill_tracker.py
    → 체결 반영

13. exit_engine.py
    → 청산 판단

14. position_reconciler.py
    → 상태 동기화

15. state_manager.py
    → atomic 저장

16. ledger_writer.py
    → 거래 기록

17. exception_handler.py
    → 예외 발생 시 SAFE_HALT
	
	
시간 구조 (고정)	
08:00              → UNIVERSE 생성

09:00 ~ 14:50      → TRADE (진입 + 청산)

14:50 ~ 15:00      → NO_ENTRY (진입 금지, 청산만)

15:00 ~ 15:30      → FORCE_EXIT (전량 청산)

15:30 이후         → CLOSED (완전 종료)

운영 고정 규칙

- 전략은 STRATEGY에서만 수정
- LOCKED 절대 수정 금지
- 전종목 스캔 금지
- Top-N → 3~5 종목만 사용
- 최대 2포지션
- 동일 자산군 금지
- state 저장은 atomic write 필수
- 예외 발생 시 무조건 SAFE_HALT
- recovery_policy 기반 강제 종료


한 줄 최종 결론
IRONCLAD_V30.2_FINAL은
전략(STRATEGY)과 실행(RUNTIME)을 완전히 분리하고,
UNIVERSE로 종목을 고정하며,
LOCKED로 안전을 강제하는
실전 자동매매 최종 구조다.