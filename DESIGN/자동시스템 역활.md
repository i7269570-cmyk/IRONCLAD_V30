 1️⃣ 당신이 이해한 흐름 (맞는 부분)

API 연결
→ 데이터 가져옴
→ 종목 2개 선정
→ 자동 매매
→ 시간되면 종료
👉 **너무 단순화된 이해**

## 2️⃣ 실제 IRONCLAD 흐름 (정확한 구조)

id="flow1"
scheduler
→ data_loader (current + history)
→ selector (종목 선정)
→ regime_filter (시장 상태 필터)
→ entry_engine (진입 판단)
→ risk_gate (리스크 검증)
→ order_manager (주문 실행)
→ fill_tracker (체결 확인)
→ position_reconciler (정합성 유지)
→ exit_engine (청산 판단)
→ state_manager (상태 저장)

👉 이게 진짜 구조

## 3️⃣ 핵심 차이 (중요) -  ❌ 단순 자동매매

id="simple" / 데이터 → 조건식 → 주문

### ⭕ 시스템

id="advanced"
판단
→ 검증
→ 실행
→ 확인
→ 정합성 유지
→ 상태 기록

👉 단계가 완전히 다름


## 4️⃣ “종목 2개 선정”의 정확한 위치

👉 selector 역할
id="selector"
전체 유니버스
→ 필터링
→ 랭킹
→ top_k (예: 2개)

👉 자동으로 선택되는 구조 맞음 ✔


## 5️⃣ “자동 멈춤” 구조

👉 scheduler 역할

id="scheduler"
TRADE
NO_ENTRY
FORCE_EXIT
CLOSED
```

예:

* 장중 → TRADE
* 종료 30분 전 → FORCE_EXIT
* 장 종료 → CLOSED

👉 시간 제어 자동 ✔

## 6️⃣ API 연결 후 실제 흐름

id="realflow"
API 연결
→ 실시간 데이터 수신
→ data_loader 구성
→ selector 실행
→ 조건 충족 시 entry_engine
→ risk_gate 통과 시 주문
→ fill_tracker 체결 확인
→ exit_engine 청산
→ state_manager 기록
→ 반복


## 7️⃣ 핵심 정리

👉 당신 시스템은 id="core" / 자동 매매 프로그램” ❌ / 자동 실행 엔진” ⭕

## 8️⃣ 한 줄 핵심

👉 종목 2개 뽑아서 사고파는 시스템이 아니라
👉 **전체 흐름을 통제하는 엔진**

## 9️⃣ 지금 상태에서 중요한 포인트

👉 앞으로 할 일:

id="next"
API 연결
→ 소액 실전
→ 흐름 확인


👉 절대 할 일 아님:

id="dont"
구조 수정 ❌
엔진 건드림 ❌


## 최종 결론

👉 이해 방향은 맞다
👉 하지만 실제는 훨씬 더 정교한 시스템이다


원하면 다음 단계:

* “LS증권 + 업비트 API 연결 흐름 (파일 기준)”
* “실전 첫 실행 체크리스트”
* “실전에서 가장 먼저 터지는 부분 3개”

바로 이어서 정리해준다.
