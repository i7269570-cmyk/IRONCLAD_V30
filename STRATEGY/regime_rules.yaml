# ============================================================
# IRONCLAD_V31.3 - Market Regime Rules (Logic Sync)
# ============================================================
# ROLE: MARKET ENTRY BLOCKER (Regime Filter Sync)
# PRINCIPLE: MAJORITY EVALUATION / PASS ON THRESHOLD
# ============================================================

regime_rules:
  # [1] Evaluation Metric (String)
  # Evaluation field based on selector output (e.g., change_rate, value)
  metric: "change_rate"

  # [2] Entry Threshold (Number)
  # [Rule] PASS if MAJORITY of selected candidates' metric is GREATER THAN OR EQUAL TO this value.
  # Example: -0.01 means entry is ALLOWED if 50% or more candidates have change_rate >= -1%.
  threshold: -0.01

# ------------------------------------------------------------
# OPERATIONAL IMPACT:
# 1. Logic: Evaluate each candidate's 'metric' against 'threshold' independently.
# 2. Condition: If (Majority of candidates' metric >= threshold) -> ENTRY ALLOWED (PASS).
# 3. Restriction: Average-based filtering or single-candidate pass logic is strictly prohibited.
# ------------------------------------------------------------